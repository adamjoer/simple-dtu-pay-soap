
In order to run the system on your local machine there are two options:

- Build and deploy on Docker containers
- Manual build and deploy

## Build and deploy with Docker

To build and deploy with Docker, you just need to run the script

```shell
./build-and-run.sh
```

